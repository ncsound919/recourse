# Recourse PDF sidecar (PyMuPDF text extraction). Start it with:
#   uvicorn main:app --host 127.0.0.1 --port 8600
# Recourse keeps this in sync with PDF_SIDECAR_URL (default http://127.0.0.1:8600).
PDF_PORT=8600
