# Recourse fuzzy-dedup sidecar (RapidFuzz). Start it with:
#   uvicorn main:app --host 127.0.0.1 --port 8700
# Recourse keeps this in sync with FUZZ_SIDECAR_URL (default http://127.0.0.1:8700).
FUZZ_PORT=8700
